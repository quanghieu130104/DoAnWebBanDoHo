@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dữ liệu Cá nhân";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
  
}

<div class="main-content-wrapper">
    <div class="login-card p-4 text-start">
        @* Sử dụng login-card để định dạng và căn trái nội dung *@

        <h3 class="text-center mb-4">@ViewData["Title"]</h3>

        <div class="row justify-content-center">
            <div class="col-md-12">

                <p class="text-muted">Tài khoản của bạn chứa dữ liệu cá nhân mà bạn đã cung cấp cho chúng tôi. Trang này cho phép bạn tải xuống hoặc xóa dữ liệu đó.</p>

                <div class="alert alert-warning my-4" role="alert">
                    <p class="mb-0">
                        <strong>Cảnh báo: Việc xóa dữ liệu này sẽ xóa vĩnh viễn tài khoản của bạn và không thể khôi phục lại được.</strong>
                    </p>
                </div>

                <form id="download-data" asp-page="DownloadPersonalData" method="post" class="d-grid mb-3">
                    <button class="btn btn-lg btn-custom-dark" type="submit">
                        TẢI XUỐNG DỮ LIỆU
                    </button>
                </form>

                <div class="d-grid">
                    <a id="delete" asp-page="DeletePersonalData" class="btn btn-lg btn-danger">
                        XÓA DỮ LIỆU VÀ ĐÓNG TÀI KHOẢN
                    </a>
                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}